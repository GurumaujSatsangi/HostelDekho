<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css"
    />
  </head>
  <body>
    <%- include('partials/navbar') %>
    <nav aria-label="breadcrumb" class="bg-light rounded-3 p-3 mb-4">
  <ol class="breadcrumb mb-0">
    <li class="breadcrumb-item"><a href="/" class="text-decoration-none">Home</a></li>
    <li class="breadcrumb-item active" aria-current="page">HOSTEL DETAILS : <%= hosteldata.hostel_name %></li>
  </ol>
</nav>
    
    <div class="container">
      
      <h4>BLOCK DETAILS</h4>
      <hr />
      <table class="table">
        <tbody>
          <tr>
            <td><b>Hostel</b></td>
                        <td><b>Type</b></td>
                                    <td><b>Type of Bedrooms Available</b></td>
                                                <td><b>Bed Type</b></td>

</tr><tr>

            <td><%= hosteldata.hostel_name %></td>
            <td><%= hosteldata.hostel_type %></td>
            <td><%= hosteldata.bed_availability %></td>
            <td><%= hosteldata.bed_type %></td>



          </tr>
          
        </tbody>
      </table>
      <h4>FLOOR PLANS</h4>
      <hr />
      <table class="table">
        <tbody>
          <tr>
            <% if(floorplan && floorplan.length>0){ %> <%
            floorplan.forEach(function(floor,index){ %>

              <div class="accordion" id="accordionExample">
  <div class="accordion-item">
    <h2 class="accordion-header">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#<%=floor.floor%>" aria-expanded="false" aria-controls="collapseOne">
        <%= floor.floor %> &nbsp; <% if(!floor.link) {%>
      <span class="badge text-bg-danger">NOT AVAILABLE</span>
      <%} %>
        
      </button>
      
    </h2>
    

    <div id="<%= floor.floor%>" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
      <div class="accordion-body">
<center><img width="50%" src="<%= floor.link %>"></img>
<a href="/view/hostel/<%= hosteldata.hostel_id %>/floor-plan/<%= floor.id %>" class="btn btn-outline-primary">Explore Floor</a>
</center>
      </div>
    </div>
  </div>
 
</div>



           
            <% }); %>
          </tr>
        </tbody>
      </table>
      <% } else { %>
      <p>No Floor Plans available!</p>

      <% } %>
    </div>
    <%- include('partials/footer') %>
  </body>
</html>
